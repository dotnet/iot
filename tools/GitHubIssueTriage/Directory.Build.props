<Project>
  <!-- Override parent Directory.Build.props to avoid Arcade SDK dependency -->
  <PropertyGroup>
    <!-- Use available .NET SDK version -->
    <TargetFramework>net8.0</TargetFramework>
  </PropertyGroup>
</Project>